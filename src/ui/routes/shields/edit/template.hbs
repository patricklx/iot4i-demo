<div class="bx--grid">
  <div class='bx--row'>
    <div class='bx--col-sm-6'>
      <Carbon::Tile>
        <h4>{{shield.name}}</h4>
        <small>{{shield.id}}</small>
        <br>
        <br>
        <div style='max-width: 400px'>
          <Form-Editor
            @model={{shield}}
            @sections={{formSections}}
          />
        </div>
      </Carbon::Tile>
    </div>
    <div class='bx--col-sm-6'>
      <section class="bx--structured-list">
        <div class="bx--structured-list-thead">
          <div class="bx--structured-list-row bx--structured-list-row--header-row">
            <div class="bx--structured-list-th">{{shield.name}}: Shield Codes</div>
            <div class="bx--structured-list-th"></div>
          </div>
        </div>
        <div class="bx--structured-list-tbody">
          {{#each shield.codes as |shieldCode|}}
            <div class="bx--structured-list-row">
              <div class="bx--structured-list-td bx--structured-list-content--nowrap">
                <h3>{{shieldCode.name}}</h3>
                <p>{{shieldCode.description}}</p>
              </div>
              <div class="bx--structured-list-td">
                <Carbon::Checkbox @checked={{shieldCode.enabled}} @onChange={{action (mut shieldCode.enabled)}} />
                <Carbon::Icon @icon="edit" @info="true" @onClick={{action 'editShieldCode' shieldCode}}/>
                <span style="width: 10px; display: inline-block;"></span>
                <Carbon::Icon @icon="delete" @danger="true" @onClick={{action 'deleteShieldCode' shieldCode}}/>
              </div>
            </div>
          {{/each}}
          <div class="bx--structured-list-row">
            <div class="bx--structured-list-td bx--structured-list-content--nowrap">
              <Carbon::Button @primary="true" @onClick={{action 'addNew'}}>
                New <Carbon::Icon @icon="add" />
              </Carbon::Button>
            </div>
            <div class="bx--structured-list-td">
            </div>
          </div>
        </div>
      </section>
    </div>
  </div>
</div>