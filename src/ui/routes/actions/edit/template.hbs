<div class="bx--grid">
  <div class='bx--row'>
    <div class='bx--col-sm-6'>
      <Carbon::Tile>
        <h4>{{action.name}}</h4>
        <small>{{action.id}}</small>
        <br>
        <br>
        <div style='max-width: 400px'>
          <Form-Editor
            @model={{model}}
            @sections={{formSections}}
          />
        </div>
      </Carbon::Tile>
    </div>
  </div>
</div>