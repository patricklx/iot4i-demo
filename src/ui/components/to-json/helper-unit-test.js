
import { toJson } from 'demoapp/src\\ui\\components\\to-json\\helper';
import { module, test } from 'qunit';

module('Unit | Helper | to json');

// Replace this with your real tests.
test('it works', (assert) => {
  const result = toJson([42]);
  assert.ok(result);
});
