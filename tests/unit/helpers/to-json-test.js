
import { toJson } from 'demoapp/helpers/to-json';
import { module, test } from 'qunit';

module('Unit | Helper | to json');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toJson([42]);
  assert.ok(result);
});

